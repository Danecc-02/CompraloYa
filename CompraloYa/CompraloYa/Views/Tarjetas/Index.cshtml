@model IEnumerable<CompraloYa.Models.Tarjetas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumeroTarjeta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaExpiracion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroTarjeta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaExpiracion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VCCtarjeta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VCCtarjeta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VCCtarjeta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
