@model IEnumerable<CompraloYa.Models.TypeRopa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeRopaName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeRopaName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTypeRopa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTypeRopa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTypeRopa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
